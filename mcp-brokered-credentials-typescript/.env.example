# Runtime config for the MCP proxy itself.
# Copy to .env and fill in. KEYCARD_CLIENT_ID / KEYCARD_CLIENT_SECRET are
# brokered into the process by `keycard run` from the zone vault — do NOT
# add them here.

KEYCARD_URL=https://<your-zone-id>.keycard.cloud
PORT=8000
